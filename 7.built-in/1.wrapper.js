// Wrapper Object
// 원시값을 필요에 따라서 관련된 빌트인 객체로 변환한다.
const number = 123; // number is primitive type
console.log(number.toString());

const text = 'text'; // string is primitive type
console.log(text);
text.length;
text.trim();
